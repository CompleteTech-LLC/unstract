{
  "title": "Weaviate Vector DB",
  "description": "Local Docker defaults are pre-filled for the bundled anonymous Weaviate service. Replace them when connecting to Weaviate Cloud or another server.",
  "type": "object",
  "required": [
    "adapter_name",
    "url"
  ],
  "properties": {
    "adapter_name": {
      "type": "string",
      "title": "Name",
      "default": "weaviate-local",
      "description": "Provide a unique name for this adapter instance. Example: weaviate-vdb-1"
    },
    "url": {
      "type": "string",
      "title": "URL",
      "default": "http://weaviate:8080",
      "format": "uri",
      "description": "The URL of the Vector DB instance. Local Docker: http://weaviate:8080. Cloud example: https://<your-instance>.weaviate.network"
    },
    "api_key": {
      "type": "string",
      "title": "Api Key",
      "default": "",
      "format": "password"
    }
  }
}
