{
  "title": "Qdrant Vector DB",
  "description": "Local Docker defaults are pre-filled for the bundled Qdrant service. Replace them when connecting to another Qdrant server.",
  "type": "object",
  "required": [
    "adapter_name",
    "url"
  ],
  "properties": {
    "adapter_name": {
      "type": "string",
      "title": "Name",
      "default": "qdrant-local",
      "description": "Provide a unique name for this adapter instance. Example: qdrant-vdb-1"
    },
    "url": {
      "type": "string",
      "title": "URL",
      "default": "http://qdrant:6333",
      "pattern": "(http|https)?[\\w\\d\\-\\.:]+"
    },
    "api_key": {
      "type": "string",
      "title": "API Key",
      "default": "",
      "format": "password"
    }
  }
}
