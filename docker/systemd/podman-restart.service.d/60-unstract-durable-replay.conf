[Unit]
# podman-restart.service is the existing generic owner for the Unstract
# containers' unless-stopped policy. Require the guarded Compose replay first
# so a manual or boot-triggered generic restart cannot bypass durable state.
Requires=unstract-durable-replay.service
After=unstract-durable-replay.service
